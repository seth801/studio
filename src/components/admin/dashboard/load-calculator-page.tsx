
'use client';

import { Card, CardContent, CardHeader, CardTitle, CardDescription } from '@/components/ui/card';

export function LoadCalculatorPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Load Calculator</CardTitle>
        <CardDescription>
          Calculate the estimated profit for a load.
        </CardDescription>
      </CardHeader>
      <CardContent>
        <p>Load calculator functionality coming soon.</p>
      </CardContent>
    </Card>
  );
}
